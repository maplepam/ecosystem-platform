name: ecosystem_platform_workspace
description: Shared EMP AI platform packages (foundation, core, design system, app shell).
publish_to: none

environment:
  sdk: ">=3.0.5 <4.0.0"

dev_dependencies:
  melos: ^6.3.2
